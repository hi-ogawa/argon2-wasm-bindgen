[package]
name = "argon2_wasm_bindgen"
version = "0.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = { version = "0.2.86" }
argon2 = { version = "0.5.0" }
password-hash = { version = "0.5" }
